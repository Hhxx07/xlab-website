# World GLB Models

Place optional house models here and set `modelUrl` in `src/features/world/data/worldModules.ts`.

Example:

```ts
modelUrl: '/world/models/knowledge.glb'
```

When `modelUrl` is omitted, the pixel fallback house is rendered.
